Python crash course 

1. open Jupyter notebook inside Anaconda terminal
  - type jupyter notebook
    - opens in browser
      - click ( Desktop )
        - click New
          - create new Python 3 notebook, set notebook name

2. Jupyter notebook function test
  - type (1+1)
    - click Run or hit ( Shift + Enter )
      - to delete a line hit ( ESC + (d key twice) )
